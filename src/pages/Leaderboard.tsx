import { Grid, Typography } from "@mui/material";


export default function Leaderboard() {
  return (
    <Grid id="leaderboard" container direction="column" pt={20} pb={30}>
      <Typography variant="h2">Ribbit Leaderboard</Typography>
    </Grid>
  )
}